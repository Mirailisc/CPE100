#include <stdio.h>

int main()
{
	int a = 0, b;
	float c = 2.5;
	// ! 'A'
	char d = "A";

	// ! semi-colon is missing
	double e

	// ! 'D'
	d = D;
	e = 10;

	// ! printf("Enter variable b: \n");
	printf('Enter variable b: /n');

	// ! scanf("%d", &b)
	scanf(%d), b;

	// ! printf("a = %d, b = %d, c = %lf, d = %c, e = %f", a, b, c, d, e);
	printf("a = %d, b = %f, c = %lf, d = %c, e = %d");

	return 0;
}
